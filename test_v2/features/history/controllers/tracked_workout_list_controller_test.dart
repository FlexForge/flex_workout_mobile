import 'package:flutter_test/flutter_test.dart';

void main() {
  group(
    skip: 'Group is unimplemented',
    'TrackedWorkoutListController',
    () {
      group('build', () {});

      group('addWorkout', () {});
    },
  );

  group(
    skip: 'Group is unimplemented',
    'HashMapGenerator',
    () {
      group('getHashCode', () {});

      group('toDateTimeMap', () {});

      group('toHash', () {});
    },
  );

  group(
    skip: 'Group is unimplemented',
    'TrackedWorkouttHistoryListToMap',
    () {
      group('toFullMap', () {});

      group('toSingleMap', () {});
    },
  );

  group(
    skip: 'Group is unimplemented',
    'WorkoutSectionTile',
    () {
      group('toWorkoutHistoryTable', () {});
    },
  );
}
