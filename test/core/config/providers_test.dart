import 'package:flutter_test/flutter_test.dart';

void main() {
  group(
    skip: 'Group is unimplemented',
    'Providers',
    () {
      group('objectBoxStore', () {});
      group('sharedPreferences', () {});
      group('initializeProviders', () {});
    },
  );
}
