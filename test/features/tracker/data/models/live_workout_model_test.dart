import 'package:flutter_test/flutter_test.dart';

void main() {
  group('LiveWorkoutModel', () {});

  group('ILiveSection', () {
    group('LiveDefaultSectionModel', () {
      group('generateTitle', () {});
      group('completedSets', () {});
      group('display', () {});
      group('getExercises', () {});
      group('getVolume', () {});
      group('getCompletedSets', () {});
      group('toEntity', () {});
    });

    group('LiveSupersetSectionModel', () {
      group('generateTitle', () {});
      group('allSets', () {});
      group('completedSets', () {});
      group('completedSetsMap', () {});
      group('display', () {});
      group('getExercises', () {});
      group('getVolume', () {});
      group('getCompletedSets', () {});
      group('toEntity', () {});
    });

    group('LiveDefaultSetModel', () {
      group('getLoadInKgs', () {});
      group('getLoadInLbs', () {});
      group('getVolume', () {});
      group('display', () {});
      group('toEntity', () {});
    });
  });
}
