import 'package:flutter_test/flutter_test.dart';

void main() {
  group(
    skip: 'Group not implemented',
    'LiveWorkoutController',
    () {
      group('build', () {});
      group('addDefaultSection', () {});
      group('addSupersetSection', () {});
      group('addSet', () {});
      group('completeDefaultSet', () {});
      group('removeSection', () {});
      group('removeDefaultSet', () {});
      group('getTotalVolume', () {});
      group('getSetsCompleted', () {});
    },
  );
}
