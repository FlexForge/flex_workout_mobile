import 'package:flutter_test/flutter_test.dart';

void main() {
  group(
    skip: 'Group is unimplemented',
    'UserRepository',
    () {
      group('getUser', () {});

      group('createUser', () {});

      group('updateTheme', () {});
    },
  );
}
